---
features:
  - |
    Add pure virtual functions ``vartype()``, ``lower_bound()`` and ``upper_bound()``
    to ``dimod::QuadraticModelBase``, thereby making it an abstract class.
  - |
    Add ``dimod::QuadraticModelBase::add_quadratic()`` overload for dense matrices.
upgrade:
  - |
    ``dimod::QuadraticModelBase`` is now an abstract class.
