---
features:
  - |
    Add a pure virtual function ``vartype()`` to ``dimod::QuadraticModelBase``,
    thereby making it an abstract class.
upgrade:
  - |
    ``dimod::QuadraticModelBase`` is now an abstract class.
