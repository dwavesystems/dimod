---
features:
  - |
    Added `dimod.generators.magic_square` which allows for the generation of a `ConstrainedQuadaticModel` whos feasible 
    solutions are normal magic squares. The function also generated magic squares of squares when passed `power=2` as a key-word argument.
